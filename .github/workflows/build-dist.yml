name: Build dist from dev

on:
  push:
    branches:
      - dev
    paths-ignore:
      - agent-knowledge/**

permissions:
  contents: write

concurrency:
  group: build-dist-main
  cancel-in-progress: false

jobs:
  build:
    name: Build dist from dev
    runs-on: ubuntu-latest
    steps:
      - name: Checkout triggering dev commit
        uses: actions/checkout@v7.0.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Checkout Yunzai
        uses: actions/checkout@v7.0.1
        with:
          repository: bling-yshs/Yunzai
          path: yunzai
          fetch-depth: 1

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.1.0
        with:
          version: 12.4.2

      - name: Setup Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 24

      - name: Copy plugin into Yunzai workspace
        run: rsync -a --delete --exclude='.git/' ./ yunzai/plugins/mhy-plugin/

      - name: Install Yunzai workspace dependencies
        working-directory: yunzai
        run: pnpm install --no-frozen-lockfile

      - name: Build plugin
        working-directory: yunzai
        run: pnpm --filter mhy-plugin run build

      - name: Upload dist
        uses: actions/upload-artifact@v7.0.1
        with:
          name: dist
          path: yunzai/plugins/mhy-plugin/dist
          if-no-files-found: error
          retention-days: 1

  publish:
    name: Sync dev and dist to main
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v7.0.1
        with:
          ref: main
          fetch-depth: 0

      - name: Refresh main
        run: git pull --ff-only origin main

      - name: Configure Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Merge built dev commit
        run: |
          git fetch origin "$GITHUB_SHA"
          git merge --no-ff --no-commit "$GITHUB_SHA"

      - name: Clear existing dist
        run: rm -rf dist

      - name: Download dist
        uses: actions/download-artifact@v8.0.1
        with:
          name: dist
          path: dist

      - name: Commit source and dist
        env:
          DEV_COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
        run: |
          git add -A --force dist
          if git diff --cached --quiet; then
            echo "dist is up to date"
            exit 0
          fi
          printf '%s\n' "$DEV_COMMIT_MESSAGE" > "$RUNNER_TEMP/commit-message"
          git commit -F "$RUNNER_TEMP/commit-message"
          git push origin HEAD:main
